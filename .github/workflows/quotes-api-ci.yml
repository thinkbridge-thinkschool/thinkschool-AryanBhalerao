name: QuotesAPI CI

on:
  push:
    branches: ["main", "master"]
    paths:
      - "Day3/piece3/**"
      - "Day3/piece5/**"
      - ".github/workflows/quotes-api-ci.yml"
  pull_request:
    branches: ["main", "master"]
    paths:
      - "Day3/piece3/**"
      - "Day3/piece5/**"
      - ".github/workflows/quotes-api-ci.yml"

jobs:
  test:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Restore (integration tests)
        run: dotnet restore Day3/piece3/QuotesApi.Tests/QuotesApi.Tests.csproj

      - name: Build (integration tests)
        run: dotnet build Day3/piece3/QuotesApi.Tests/QuotesApi.Tests.csproj --no-restore --configuration Release

      - name: Test (integration tests)
        run: dotnet test Day3/piece3/QuotesApi.Tests/QuotesApi.Tests.csproj --no-build --configuration Release --verbosity normal

      - name: Restore (unit tests)
        run: dotnet restore Day3/piece5/Quotes.Tests.Unit/Quotes.Tests.Unit.csproj

      - name: Build (unit tests)
        run: dotnet build Day3/piece5/Quotes.Tests.Unit/Quotes.Tests.Unit.csproj --no-restore --configuration Release

      - name: Test (unit tests)
        run: dotnet test Day3/piece5/Quotes.Tests.Unit/Quotes.Tests.Unit.csproj --no-build --configuration Release --verbosity normal
